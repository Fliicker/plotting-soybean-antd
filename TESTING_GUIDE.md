# 属性表功能测试指南

## 🔧 已修复的问题

### 1. 属性表可拖拽和调整大小
- ✅ 添加了 `draggable` 和 `resizable` 属性
- ✅ 移除了遮罩层 (`mask="false"`) 以便更好的交互
- ✅ 添加了拖拽指针样式和调整大小手柄
- ✅ 设置了最小尺寸 (600px × 400px)

### 2. 缩放到图层功能
- ✅ 修复了"该图层没有预设的视图范围"问题
- ✅ 智能计算图层边界：
  - 优先使用预设的 `viewState`
  - 通过查询要素计算实际边界
  - 支持各种几何类型 (Point, LineString, Polygon, Multi*)
  - 回退到渲染要素查询
  - 最终回退到默认视图

### 3. 错误处理和调试
- ✅ 修复了所有 TypeScript 错误
- ✅ 改进了 `querySourceFeatures` 调用参数
- ✅ 增强了错误处理和用户反馈
- ✅ 添加了详细的调试日志

## 🧪 测试步骤

### 测试1: 属性表基本功能
1. 从数据目录添加一个矢量图层到图层管理面板
2. 右键点击图层管理面板中的图层
3. 选择"查看属性表"
4. 验证：
   - ✅ 属性表弹出并显示数据
   - ✅ 可以拖拽标题栏移动窗口
   - ✅ 可以从右下角拖拽调整大小
   - ✅ 表格数据正确显示

### 测试2: 搜索和过滤
1. 在属性表中选择一个字段
2. 输入搜索值
3. 验证：
   - ✅ 数据正确过滤
   - ✅ 计数更新正确

### 测试3: 要素定位
1. 点击属性表中的任意行
2. 验证：
   - ✅ 地图自动定位到对应要素
   - ✅ 缩放级别合适

### 测试4: 缩放到图层
1. 右键点击图层管理面板中的图层
2. 选择"缩放到图层"
3. 验证：
   - ✅ 地图缩放到图层范围
   - ✅ 显示成功消息
   - ✅ 无错误提示

### 测试5: 导出功能
1. 在属性表中点击"导出"按钮
2. 验证：
   - ✅ 成功下载 CSV 文件
   - ✅ 文件名包含图层名称
   - ✅ 数据格式正确

## 🐛 错误场景测试

### 测试6: 无数据图层
1. 添加一个没有要素的图层
2. 查看属性表
3. 验证：
   - ✅ 显示友好的错误消息
   - ✅ 不会崩溃

### 测试7: 未激活图层
1. 添加图层但不勾选
2. 右键查看属性表
3. 验证：
   - ✅ 提示需要先激活图层

### 测试8: 网络错误
1. 断开网络连接
2. 尝试查看属性表
3. 验证：
   - ✅ 显示适当的错误消息
   - ✅ 应用不会崩溃

## 🔍 调试信息

### 控制台日志
查看浏览器控制台，应该看到详细的查询日志：
```
开始查询图层 layer_id (图层名称) 的要素...
✅ 成功获取 N 个要素 (方法: MapScene.queryLayerFeatures)
```

### 支持的查询方法
1. `MapScene.queryLayerFeatures` (优先)
2. `querySourceFeatures` (回退)
3. `queryRenderedFeatures` (最后回退)

### 图层类型支持
- ✅ 矢量图层 (点、线、面)
- ✅ 栅格图层 (基本支持)
- ✅ 3D 图层 (基本支持)
- ✅ 临时图层 (分析结果)

## 📋 已知限制

1. **栅格图层**: 只能显示基本的图层信息，没有像素级属性
2. **大数据量**: 超过1000个要素可能影响性能
3. **复杂几何**: MultiPolygon 等复杂几何可能计算边界较慢

## 🚀 性能优化

- 分页显示 (每页50条)
- 懒加载要素属性
- 智能查询方法选择
- 缓存查询结果

## 📝 开发者注意事项

- 所有查询都有错误处理和回退机制
- 用户界面响应式设计，适配不同屏幕尺寸
- 符合现有代码风格和架构规范
- 完整的 TypeScript 类型支持
